import {safeStorage} from 'electron'; import {mkdir,readFile,rm,writeFile} from 'node:fs/promises'; import {join} from 'node:path'; import type {SecureTokenStore} from './token-store.js';
export class ElectronSecureTokenStore implements SecureTokenStore{constructor(private directory:string){}private path(k:string){return join(this.directory,`${k}.dat`)}async get(k:string){try{return safeStorage.decryptString(await readFile(this.path(k)))}catch{return null}}async set(k:string,v:string){if(!safeStorage.isEncryptionAvailable())throw new Error('Stockage sécurisé indisponible');await mkdir(this.directory,{recursive:true});await writeFile(this.path(k),safeStorage.encryptString(v),{mode:0o600})}async delete(k:string){await rm(this.path(k),{force:true})}}
