import js from '@eslint/js'; import tseslint from 'typescript-eslint'; import hooks from 'eslint-plugin-react-hooks';
export default tseslint.config({ignores:['dist','dist-electron','release','node_modules']},js.configs.recommended,...tseslint.configs.recommended,{files:['**/*.{ts,tsx}'],rules:{'no-undef':'off'}},{files:['src/renderer/**/*.{ts,tsx}'],plugins:{'react-hooks':hooks},rules:hooks.configs.recommended.rules},{rules:{'@typescript-eslint/no-explicit-any':'off'}});
